- content_for :top_right do
  = link_to 'Add a new server', new_server_path, class: "btn large"

%h1 Authorized Email Servers

%table.table.table-striped.table-hover.table-bordered
  %thead
    %tr
      %th Name
      %th Post URL
  %tbody
    = render @servers
