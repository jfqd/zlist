- if logged_in?
  %h1 New subscriber
= form_for @subscriber do |f|
  = f.error_messages
  = render "form", f: f

  .actions
    = f.submit "Create", class: "btn primary"
    = link_to "Cancel", subscribers_path, class: "btn"
