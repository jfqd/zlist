- content_for :top_right do
  = link_to 'Add a new subscriber', new_subscriber_path, class: "btn large"

= render 'search'

%table.table.table-striped.table-hover.table-bordered
  %thead
    %tr
      %th Name
      %th Lists
  %tbody#subscribers
    = render @subscribers 

= will_paginate @subscribers
