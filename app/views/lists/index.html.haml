- content_for :top_right do
  = link_to 'Add a new list', new_list_path, class: "btn large"

%h1 Mailing Lists

%table.table.table.table-striped.table-hover.table-bordered
  %thead
    %tr
      %th Name
      %th Mailbox
      %th Subscription
      %th Topics
  %tbody
    = render @lists
